import { jsx } from '@emotion/core';
import { FieldContainer, FieldLabel, FieldInput } from '@arch-ui/fields';
import { TextDayTimePicker } from '@arch-ui/day-picker';
/** @jsx jsx */

const DateTimeField = _ref => {
  let autoFocus = _ref.autoFocus,
      field = _ref.field,
      onChange = _ref.onChange,
      value = _ref.value,
      errors = _ref.errors;
  const htmlID = "ks-input-".concat(field.path);
  return jsx(FieldContainer, null, jsx(FieldLabel, {
    htmlFor: htmlID,
    field: field,
    errors: errors
  }), jsx(FieldInput, null, jsx(TextDayTimePicker, {
    id: htmlID,
    date: value,
    onChange: onChange,
    autoFocus: autoFocus
  })));
};

export default DateTimeField;
